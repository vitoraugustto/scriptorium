import { PAGES_PER_CODEX, AUTO_TICK_MS, SCRIBE_TITLES, LOREM } from './constants.js';
import { FOLIO_LAYOUTS, FOLIO } from './layouts.js';
import { GOLD_UPGRADES, SALT_UPGRADES } from './upgrades.js';

export default Object.freeze({
  PAGES_PER_CODEX,
  AUTO_TICK_MS,
  SCRIBE_TITLES,
  LOREM,
  FOLIO,
  FOLIO_LAYOUTS,
  GOLD_UPGRADES,
  SALT_UPGRADES,
});

import { setLayout, refreshFolio, clearFolio, initRules, countRedWords } from './folio.js';
import { fmt, fmtSalt, refreshStats, flashKey, spawnFloat, showToast } from './hud.js';
import { refreshUpgrades } from './upgrades.js';

export default {
  setLayout, refreshFolio, clearFolio, initRules, countRedWords,
  fmt, fmtSalt, refreshStats, flashKey, spawnFloat, showToast,
  refreshUpgrades,
};

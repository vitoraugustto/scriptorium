import { setLayout, refreshFolio, clearFolio, initRules, countRedWords } from './folio';
import { fmt, fmtSalt, refreshStats, refreshStaticLabels, flashKey, spawnFloat, showToast } from './hud';
import { refreshUpgrades } from './upgrades';

export default {
  setLayout, refreshFolio, clearFolio, initRules, countRedWords,
  fmt, fmtSalt, refreshStats, refreshStaticLabels, flashKey, spawnFloat, showToast,
  refreshUpgrades,
};

import { setLayout, refreshFolio, clearFolio, initRules, countRedWords } from './folio/index';
import { fmt, fmtSalt, refreshStats, refreshStaticLabels, flashKey, spawnFloat, showToast } from './hud/index';
import { refreshUpgrades } from './upgrades/index';

export default {
  setLayout, refreshFolio, clearFolio, initRules, countRedWords,
  fmt, fmtSalt, refreshStats, refreshStaticLabels, flashKey, spawnFloat, showToast,
  refreshUpgrades,
};

# Sound credits

All files are converted to AAC (`.m4a`) from the originals.

| File | Source | Author |
|------|--------|--------|
| `quill.m4a` | Freesound — "Writing Pen" | Rui Aires |
| `page-turn.m4a` | Freesound — "Turn Page" | Alixgaus |
| `upgrade.m4a` | Freesound — "Upgrade Select UI" | Techspired Minds |
| `codex-bind.m4a` | *placeholder — silent, pending a real recording* | — |

`quill.m4a` is an 11.6 s recording played back in short random slices
(see `SPECS` in `src/renderer/js/sound/sound.ts`), so repeated keystrokes
never replay the same fragment.

Verify each source's licence on Freesound before distributing a build.

DEBUG=false

# Fine-grained PAT — used by GitHub MCP server for API calls (create PRs, comments, etc.)
GITHUB_TOKEN=

# Classic PAT with public_repo scope — used by /rabbit for GraphQL (reply + resolve threads)
GITHUB_CLASSIC_TOKEN=
